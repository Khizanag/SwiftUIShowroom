import CoreGraphics

extension DesignSystem {
    enum CornerRadius {
        /// 6 pt.
        static let small: CGFloat = 6
        /// 10 pt.
        static let medium: CGFloat = 10
        /// 16 pt — cards and panels.
        static let large: CGFloat = 16
        /// 24 pt.
        static let xLarge: CGFloat = 24
    }
}
